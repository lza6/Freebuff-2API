# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# 运行产物
*.exe
*.log
/desktop/dist/
/desktop/node_modules/
node_modules/

# 本地数据
data/
*.sqlite
*.sqlite3
*.db

# 系统
.DS_Store
Thumbs.db

# 运行时数据（不入库）
/data/
config.json
*.sqlite
*.sqlite-shm
*.sqlite-wal

# 桌面壳构建产物/依赖（不入库）
desktop/node_modules/
desktop/dist/
graft/
宣传视频/
*.exe
config2.json

# 本机 cargo 代理配置（含本地端口，不入库）
.cargo/

# E2E 运行时生成的隔离配置（可能临时含生成的 api key，绝不入库）
tests/e2e_phase_g.config.json
