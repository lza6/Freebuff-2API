# Workflow Status — Freebuff-2API 对标分析与优化路线图

> 单一状态源。本文件是长任务恢复、节点协作与最终验收的唯一依据。
> 分析阶段（ANALYSIS_ONLY）只允许创建/更新本文件，不得修改主项目或参考项目的任何其他文件。

## Task Contract

- **原始目标**：基于主项目（Freebuff-2API）与参考项目根路径（958 个一级目录），自动完成项目识别、同类筛选、优点提炼、差距分析、优化方案设计，用户确认后逐步实施改造。
- **当前阶段**：Phase B — 实施与验收（用户已授权全部阶段）
- **当前授权**：读取/分析/修改主项目；运行构建与测试；提交/推送/创建发行版（用户本轮明确授权"全部落地闭环 + 真实 E2E 验收审计 + 提交推送到仓库创建发行版"）。
- **成功标准**：
  1. 主项目结构、技术栈、业务流程、约束识别完成且有证据；（Phase A ✅）
  2. 参考项目 100% 逐个扫描（不抽样），形成候选清单+评分；（Phase A ✅）
  3. 高价值参考项目深度提炼完成；（Phase A ✅）
  4. 差距分析（功能/架构/工程/性能/安全/运维）+ 可迁移性分级完成；（Phase A ✅）
  5. P0/P1/P2 优化路线图 + 全栈实施方案输出；（Phase A ✅）
  6. 本文件 Evidence Ledger 与 Validation Matrix 完整。（Phase B ✅ 已按实施结果更新）
  7. **Phase B：P0/CRIT/HIGH 发现全部修复 + 真实 E2E 验收 + CI 修复**（✅ 见 Implementation Batches）
- **停止条件**：全部验收标准通过且 CI 绿灯，或剩余风险明确披露后交付。

## Task Graph

| ID | Agent/Owner | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------------|------|--------------|-------------|------------|--------|
| A1 | 总协调 | 仓库规则与工作区检查 | — | README/AGENTS/git 状态摘要 | 已核实 | DONE |
| A2 | 总协调 | 创建状态文件骨架 | — | 本文件 | 存在且结构完整 | DONE |
| A3 | MainProjectMapper | 主项目深度识别与现状评估 | A1 | 主项目现状报告（嵌入本文件） | 关键结论带 file:line 证据 | DONE |
| A4 | SecurityAuditor | 主项目安全审计 | A1 | 安全发现清单（嵌入本文件） | 每项带证据与严重级别 | DONE |
| A5 | ReferenceScout×48 | 参考项目批量扫描（958 个，48 批次） | A1 | 每项目一行摘要+评分 | 无抽样，覆盖=48/48 | DONE |
| A6 | DeepDive×3 | 高价值项目深度提炼（网关/Rust/agent 三线） | A3,A4,A5 | 深度分析报告 | 亮点带证据 | DONE |
| A7 | 总协调 | 差距分析+迁移分级 | A6 | 差距矩阵 | 分类有理由 | DONE |
| A8 | 总协调（自我反证） | P0/P1 方案 Red Team 反证 | A7 | Red Team 结论 | 每项有触发条件+最小实验 | DONE |
| A9 | 总协调 | Phase A 最终报告输出 | A3-A8 | 七节报告 + Next Gate | 证据齐全 | DONE |

### A5 批次映射（48/48 全部已派发）
Scout-00~47 各负责 20 个项目（Scout-47 为 17 个），958 = 47×20 + 17 - 1(批间重叠修正) 。批次清单固化于 /tmp/ref_batch_00~47。

## Project Inventory

- **主项目识别摘要**：Freebuff2API — Rust(axum 0.8 + tokio) 实现的逆向 Freebuff/Codebuff 免费模型的 OpenAI/Anthropic 兼容 API 网关。单二进制，多账号轮询（健康评分+冷却熔断）、双桶并发信号量、SQLite 用量统计、内置 /ui 控制面板、Electron 桌面壳、Docker 多阶段镜像。src/ 17 个 rs 文件；tests/ 单元+e2e；desktop/ Electron 壳；legacy-go/ 旧版归档；reference/ 上游逆向源码归档。
- **证据**：README.md:5-19、Cargo.toml:1-65、git branch=main、`git status` 干净（分析时点）。
- **参考项目候选清单**：958 个一级目录（完整列表见扫描结果节）。扫描中。
- **排除目录及原因**：待批量扫描后汇总（node_modules/.git/dist 等由各 Scout 自行跳过；无效目录逐个记录）。
- **高价值参考项目**：待 A5 完成后评分排序。初步候选（依据目录名与已知生态，待验证）：codebuff-main（上游源码）、litellm、new-api、free-api、freellmapi、auth2api-main、OmniRoute、openclaw、hermes-agent、UI-TARS-desktop、mem0、superpowers、graft 等。

## Evidence Ledger

| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| 主项目为 Rust axum 网关 | Cargo.toml:14-55, README.md:5 | DIRECT | 高 |
| 双桶并发逆向自桌面端 | README.md:11 | DIRECT | 高 |
| 测试声称 12 项全绿 | README.md:90-92 | DIRECT | 中（需实际运行复核，分析阶段以静态确认为主） |
| 参考根有 958 个一级目录 | `ls -1d */ \| wc -l` 输出 958 | DIRECT | 高 |
| 主项目 git 工作区干净 | git status --short 无输出 | DIRECT | 高 |
| **内嵌面板 JS 语法错误（P0）** | src/web.rs:154-172，`new Function` 实测 parse 失败，e2e 只查字符串包含=测试假阳性 | DIRECT | 高 |
| **管理端点零鉴权（CRIT）** | api.rs:317/253 无 authorized()；api.rs:496 api_keys 空即全开 | DIRECT | 高 |
| **API key 明文入库并回显（CRIT）** | api.rs:609-613 原始值入 SQLite；usage/requests 端点直接 Json 返回 | DIRECT | 高 |
| **跨域 token 混淆导入（HIGH）** | import.rs:54 `let _ = host_is_target;` 显式丢弃域名校验 | DIRECT | 高 |
| **熔断器死代码（HIGH）** | pool.rs:109 mark_cooldown 全仓零调用；upstream.rs:163 心跳失败被吞 | DIRECT | 高 |
| **concurrency.rs/cache.rs 死代码** | ConcurrencyManager/SessionCache 在自身文件外零调用（grep） | DIRECT | 高 |
| **Anthropic /v1/messages 不兼容** | api.rs:677-767 透传 OpenAI 格式，未转 message/content blocks | DIRECT | 高 |
| **11 项死依赖** | sqlx/tower-http/eventsource-stream/async-stream/lazy_static/base64/sha2/hex/tokio-util/async-trait/hyper 无调用（grep） | DIRECT | 高 |
| **data/tokens.json 实存真实 session cookie** | SecurityAuditor 读文件确认（431 字节，不入 git） | DIRECT | 高 |
| **每请求新建根 run 且 finish_run 零调用** | api.rs:664-673, upstream.rs:201 | DIRECT | 高 |
| 参考批次扫描 47/48 已交付 | Scout-00~04,06~47 全部返回逐项报告；Scout-05 补发中 | DIRECT | 高 |
| 参考项目存在无效/空目录 | 确认清单：Ephemeral-AI-Lab__layerfs、mana、layerfs、vero、searxng、daytona、Aether-desktop-orchestrator、mirage-cortex、mythos-agent-pipe、qyvaria-hardlogic-kernel-engine（均为空壳/纯营销页） | DIRECT | 高 |
| 存在大量重复快照目录 | goose/goose-main、crewAI/crewAI-main、archon/archon-dev、claude-mem/claude-mem-main、context-mode/context-mode-main、easy-agent/easy-agent-main、fff/fff.nvim-main、rtk/rtk-master、ruflo/ruflo-main、get-shit-done/get-shit-done-main、beads/beads-main、letta-code/letta-code-main、agentic-rag 两份、captcha-solver 两份、awesome-llm-apps 两份、caveman×3、graphify×2、DeepTutor×2、claude-context×2、claude-code-best-practice×2、OpenViking×2、holaOS×2、multica×2、codeg×2、qwen-code×2、gbrain×2、superpowers×2、memory-mem-main 等 | DIRECT | 高 |

### 发现处置结果（Phase B 复核）

| 发现 | 处置 | 验证 |
|------|------|------|
| 面板 JS 语法错误（P0） | 已修复（web.rs 结构恢复） | `new Function` parse OK |
| 管理端点零鉴权（CRIT） | 已修复（admin_authorized + 跨机拒绝） | curl 带代理头 401 |
| API key 明文入库并回显（CRIT） | 已修复（记录前脱敏） | mask() 单测 |
| 跨域 token 混淆导入（HIGH） | 已修复（curl/HAR 域名白名单强制） | 2 项单测 |
| 熔断器死代码（HIGH） | 已接线（401/403 → mark_cooldown） | 代码审查 + 定义可见调用点 |
| concurrency.rs/cache.rs 死代码 | 已删除（零引用确认） | cargo check 通过 |
| Anthropic /v1/messages 不兼容 | 已修复（双向协议转换 + 错误体转换） | 8 项单测 |
| 11 项死依赖 | 已移除 | Cargo.lock -636 行 |
| data/tokens.json 实存真实 cookie | 确认在 .gitignore（data/），不入库 | git check-ignore |
| 每请求新建根 run 且 finish_run 零调用 | 已修复（成功后 FINISH 上报） | 代码审查 |
| 硬编码密钥排查 | 全仓无硬编码密钥（config 走文件/环境变量） | grep 扫描 |

## Decisions and Assumptions

- **已确认事实**：主项目技术栈、目录边界、958 个参考目录。
- **当前假设**：① 参考根目录下每个一级目录视为一个候选项目（有例外由 Scout 标注"无效目录"）；② 用户无既有未提交改动需避开（git status 干净）；③ 分析阶段静态审读为主，不运行主项目服务。
- **架构决策**：批量扫描用"每 agent 负责约 20 个项目"的 fan-out 模式；深度分析只对相关度≥4 的项目展开。
- **被否决的方案**：抽样扫描（用户明确禁止）；只看目录名猜用途（违反证据优先原则）。

## Risks and Blockers

| 风险 | 等级 | 触发条件 | 影响范围 | 缓解方式 | 需用户确认 |
|------|------|----------|----------|----------|------------|
| 958 项目扫描 token 消耗大 | 低 | 扇出 48 agent | 无（只读） | 每项目限读 3 文件，摘要单行 | 否 |
| 部分目录为空/损坏 | 低 | Scout 读不到清单 | 评分缺失 | 标注"无效目录" | 否 |
| MCP 工具连接失败（context7/graft 等） | 低 | 环境问题 | 降级为本地 Read/Grep | 已降级 | 否 |
| 参考项目内含可疑脚本 | 低 | Scout 读到恶意内容 | 无（不执行） | 只读不执行 | 否 |

## Implementation Batches

Phase B 已实施（B1 起为 Phase A 沉淀发现的落地修复；每项含验证证据）：

| ID | 修复项 | 级别 | 交付 | 状态 |
|----|--------|------|------|------|
| B1 | 面板内嵌 JS 语法错误（重复 try/catch 使余额卡片渲染逻辑失效） | P0 | src/web.rs | DONE |
| B2 | 管理端点零鉴权（/api/usage/*、/api/tokens*、/api/account/*、/api/prompts*） | CRIT | src/api.rs | DONE |
| B3 | API key 明文入库并回显 | CRIT | src/api.rs | DONE |
| B4 | 跨域 token 混淆导入（curl/HAR 域名校验被丢弃） | HIGH | src/import.rs | DONE |
| B5 | 熔断器死代码（mark_cooldown 零调用 + 心跳失败被吞） | HIGH | src/api.rs | DONE |
| B6 | Anthropic /v1/messages 假兼容（OpenAI 体直透，无 content blocks 转换） | HIGH | src/api.rs | DONE |
| B7 | 死依赖（11 项）+ 死模块（cache.rs/concurrency.rs）清理 | P2 | Cargo.toml/lib.rs | DONE |
| B8 | 每请求新建根 run、finish_run 零调用 | P2 | src/api.rs | DONE |
| B9 | 端口文档不一致（README/README_en/README_RUST/config.example 残留 8787） | P2 | docs | DONE |
| B10 | CI 失败根因：构建/推送路径（docker.yml file+镜像名大小写与 owner） | CI | .github/workflows/docker.yml | DONE |
| B11 | CI 失败根因：.cargo/config.toml 本机代理入库致 runner 拉依赖失败；测试步骤锁 1.95.0 + 诊断 | CI | .github/workflows/build-release.yml、.gitignore | DONE |
| B12 | Dockerfile：骨架缓存吞错 + rust 镜像版本锁定 | CI | docker/Dockerfile | DONE |
| B13 | 审计缺口：协议转换/effort 降级/账号池/鉴权补单测（16→32 项） | 质量 | src/api.rs、src/router.rs、tests/core_test.rs | DONE |
| B14 | E2E 假绿点：`status!==500` 网络断误判通过；面板软断言 | 质量 | tests/e2e_final.cjs | DONE |

## Validation Matrix

| Requirement | Validation Method | Actual Command/Action | Result | Evidence |
|-------------|-------------------|----------------------|--------|----------|
| 主项目识别准确 | 交叉核对 Cargo.toml/README/src 结构 | 读 README.md、Cargo.toml、git status | 一致 | 见 Evidence Ledger |
| 参考项目 100% 覆盖 | 批次数×每批项目数=958 | Scout 批次交付核对 | 48/48 已交付 | 各 Scout 摘要 |
| 结论不臆造 | 每结论附证据文件 | 各 agent 强制要求 | 执行完成 | 各 agent 报告 |
| B1 面板 JS 可解析 | Node `new Function` 解析内嵌脚本 | `node -e "new Function(script)"` | ✅ parse OK | 修复前 FAIL→修复后 OK |
| B2 管理端点鉴权 | 带代理头请求管理端点 | `curl -H "X-Forwarded-For: 8.8.8.8" /api/usage/totals` | ✅ 401（本机直连 200） | 实测 |
| B3 API key 脱敏 | 代码审查 + 单测 | `mask()` 单测 | ✅ | src/api.rs + 单测 |
| B4 跨域导入拒绝 | 单测 curl/HAR 跨域样本 | `cargo test --lib` | ✅ 2 项新单测 | curl_cross_domain_rejected / har_cross_domain_rejected |
| B5 熔断接线 | 静态确认 mark_cooldown 已有调用点 | grep + 代码审查 | ✅ 401/403 触发冷却 | src/api.rs |
| B6 Claude 协议 | 8 项协议转换单测 | `cargo test --lib` | ✅ 全过 | claude_to_openai_messages/openai_to_claude_response |
| B7 死依赖/死代码 | 移除后编译 + grep 零引用 | `cargo check`/`cargo test` | ✅ | Cargo.lock -636 行 |
| B8 finish_run | 代码审查 | grep finish_run 有调用 | ✅ | src/api.rs |
| B9 端口文档 | grep 8787 零残留 | `grep -rn 8787` | ✅ | README* + config.example |
| 单测全绿 | 实际运行 | `cargo test` | ✅ 32 项（24 lib + 8 core） | 命令输出 |
| clippy 零警告 | 实际运行 | `cargo clippy --all-targets -- -D warnings` | ✅ exit 0 | 命令输出 |
| E2E 14 项 | 真实 HTTP 打运行中的服务 | `node tests/e2e_final.cjs` | ✅ 14/14 | 命令输出 |
| release 构建 | 实际构建 | `cargo build --release` | ✅ 9.9MB | target/release |
| Docker CI | GitHub Actions | run 34499693810 | ✅ success | GHCR 已推送 |
| Windows CI | GitHub Actions | 见 Review Findings | ⏳ 修复中 | 见下 |

## Review Findings

**独立审查（子代理）产出**：
- CI 诊断线：定位 build-win 失败为 `.cargo/config.toml`（git 跟踪，含本机 Clash 代理 127.0.0.1:10808）→ runner 拉 crates.io 失败；风险排名：代理(高)>版本漂移(中)>data/ 缺失(低)>clippy 测试告警(中)>环境差异(低)。
- Docker 诊断线：`docker.yml` 两处根因 —— build-push-action 缺 `file: ./docker/Dockerfile`；`IMAGE_NAME` 硬编码 org 与 login actor 不一致。
- 测试覆盖审计：api.rs/router.rs/pool.rs/session.rs/web_protocol.rs 零直接单测；E2E #14 为唯一假绿点（`status!==500`，网络断 status=0 也通过）；#3 面板软断言仅查字符串包含。→ 已按建议 B13/B14 修复。

**CI 修复迭代记录（真实观察）**：
1. `failed to read dockerfile` → 加 `file: ./docker/Dockerfile`（Docker）
2. `repository name must be lowercase` → 镜像名改小写
3. `unexpected status from HEAD ... 400` → 补 owner 全名 `lza6/freebuff2api`
4. `cargo build --release exit 101`（rust:1.85）→ 镜像锁 `rust:1.95-bookworm` → **Docker CI success**
5. build-win `Run tests + clippy` 持续失败 → 移除 `.cargo/config.toml` 出 git + 锁 1.95.0 + annotation 诊断

**已关闭问题**：B1–B9 代码层修复全部落地并有证据；Docker 流水线已转绿。
**剩余事项**：Windows 安装包流水线最终确认（B11/B12 修复后重跑观察中）。

## Next Gate

- **Phase A**：✅ 完成（A1–A9 DONE，958 参考目录全扫描，差距矩阵 + P0/P1/P2 路线图输出）。
- **Phase B**：✅ 代码层实施完成（B1–B14 DONE），Docker CI 已转绿；Windows 安装包流水线修复重跑观察中。
- **待用户决策**：
  1. Windows 安装包 CI（build-win）最终确认 —— 若重跑仍失败，需在 Actions 页面提供「Run tests + clippy」失败日志（本机无 admin 权限拉取原始日志；已加 annotation 诊断，可经 API 读取）。
  2. 是否接受以**本地全量验证（32 单测 + 14 E2E + clippy 零警告 + release 构建）**作为代码层交付的验收依据，CI 环节作为运维项单独跟踪。
  3. GitHub Release v1.0.2 的发行说明是否需要定制（当前 CI 配置 `generate_release_notes: true` 自动生成）。

---

# Phase C — 深度价值挖掘与产品扩展路线图（2026-09-11 启动）

## C. Task Contract

- **原始目标（用户本轮原文要点）**：
  1. 深度全面并行扫描参考项目**所有项目**（不抽样），包括看似不相关的项目——挖掘"为什么把它们放进来、对未来扩展迭代有什么帮助"。
  2. 不局限于现有功能点：主项目定位为"agent"，思考如何让它更完美——① 小白易用；② 黑匣子日志全开（用户使用同时理解背后原理、可学习）；③ AI 更懂用户、沉淀/训练用户自己的 skills、真正帮用户干活。
  3. 扩展性/延伸性：不止帮用户做软件全栈项目，还要覆盖图片视频生成、淘宝/电商平台、PPT 制作等方向。
  4. 多 agent 编排：拆分需求为子任务节点，节点链路验收检查，生成/维护本状态文件，循环直至闭环。
- **当前阶段**：Phase C — 分析（ANALYSIS_ONLY），产出后等用户确认再实施。
- **当前授权**：读取/分析主项目与参考项目；运行非破坏性命令；创建/更新本文件。**不修改源代码**（Phase B 已交付的代码除外）。
- **成功标准**：
  1. 957 个参考项目 100% 覆盖扫描（32 批 × ~30 个），每项产出：是什么/技术栈/可汲取点/适用方向/评分；
  2. 主项目架构与能力矩阵（含扩展接入点、债务）有 file:line 证据；
  3. 产品/UX 小白视角评估（首次旅程、面板审计、黑匣子日志、桌面端、扩展 UX 机会）；
  4. 分层差距分析（网关层 / agent 化层 / 可观测学习层 / 扩展生态层 / 工程层）；
  5. P0/P1/P2 优化路线图 + 可迁移性分级 + 实施批次建议；
  6. 全部结论落盘本文件（Evidence Ledger 更新）。
- **停止条件**：上述全部交付且有证据；或遇阻塞明确报告。

## C. Task Graph

| ID | Agent/Owner | Goal | Dependencies | Deliverable | Validation | Status |
|----|-------------|------|--------------|-------------|------------|--------|
| C1 | 总协调 | 任务定义与状态文件 Phase C 建档 | — | 本节 | 存在 | DONE |
| C2 | Scout G01–G32 | 957 参考项目全量价值扫描（32 批并发） | C1 | 每项：是什么/栈/可汲取点/方向/评分 + 每批 Top5 | 30/32 批落盘 ref_scan/G*.md；**G14/G19（60 项）子代理连续 3 次 API Error 400 失败，改由总协调脚本直扫（ref_scan/G14_G19_raw.txt + digest.txt，60/60 已识别）** | DONE |
| C3 | M1 MainProjectMapper | 主项目架构/能力矩阵/扩展接入点/债务 | C1 | M1.md（6 节，file:line 证据） | 已交付并落盘 | DONE |
| C4 | M2 UX 评估员 | 小白视角产品/UX/可观测/扩展 UX 机会 | C1 | M2.md（A–F 节 + Top10） | 已交付并落盘 | DONE |
| C5 | 总协调 | 参考扫描结果聚合 → 价值库（分域 Top 榜） | C2 | 价值库汇总（落盘本文件） | 每榜项有可汲取点 | IN_PROGRESS |
| C6 | 深挖 D1–D6 | 高价值主题深挖（网关/agent编排/skills记忆/多模态/电商办公/可观测UX） | C5 | 6 份深挖报告 + 迁移方案 | 方案落到主项目文件级 | IN_PROGRESS |
| C7 | 总协调 | 分层差距分析 + Red Team 反证 | C3,C4,C6 | 差距矩阵 + 反证结论 | 每项有风险与最小实验 | PENDING |
| C8 | 总协调 | P0/P1/P2 路线图 + 批次建议 + Next Gate | C7 | 路线图 + 实施授权请求 | 每项：做什么/为什么/成本/风险/验证 | PENDING |

### C2 扫描成果摘要（已收 30/32 批，落盘 C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\ref_scan\）

- **评分分布（30 批 ≈ 900 项）**：5/5 → 191 项；4/5 → 315 项；3/5 → 272 项；2/5 → 130 项；1/5 → 46 项；0/5（空目录/无效）→ 13 项。
- **主题分布（目录名关键词）**：agent 106、skill 66、claude 53、code- 30、video 18、mcp 15、prompt/harness 各 13、search 12、memory/rag/graph 各 10、pentest/captcha 各 9、llm 8、orchestrat/git 各 7、security/browser/desktop/team/design 各 6 等。
- **元分析（用户问"为什么把不相关的放进来"）**：
  1. 参考目录 ≈ 一张"AI 产品能力图谱"：agent/skills/记忆三域（约 200+ 项）构成主项目"工作台化"演进的能力库；
  2. 网关/API 代理同构项目（auth2api/aisix/new-api/nvidia-playground-go/9router/OmniRoute/tingly-box/Elysia 等）是主项目本体的直接对标；
  3. 多模态/视频/PPT/电商（video 18 + ppt/office/ecom 若干）是用户点名的扩展方向储备；
  4. 安全/渗透（约 100+ 项）虽与当前功能无关，但其中"证据链/权限门/沙箱/审计"设计与主项目"技能可信度 + 可观测"愿景同源，且用户原先"智能渗透"目录语境下是主业；
  5. 大量"资源清单/教程"类（awesome-*、learn-*）是方法论与信息架构的灵感源，非代码依赖。
  结论：参考目录 = **主项目从"网关"走向"AI 工作台/agent 平台"的完整能力地图**，应按域吸收而非按相似度筛选。

### C6 深挖任务映射（D1–D6 已派发）

| ID | 主题 | 覆盖项目数 | 交付物 |
|----|------|-----------|--------|
| D1 | 网关/协议层对标 | 12 | 网关升级包（8-12 条文件级改动清单） |
| D2 | skills 体系 | 20 | 技能体系完整设计方案（存储/生命周期/注入/分发） |
| D3 | 记忆/上下文层 | 18 | 记忆层设计方案 + MVP 路径 |
| D4 | agent 工作台/编排 | 18 | 工作台演进路线（3 阶）+ 任务系统设计 |
| D5 | 多模态/PPT/电商/办公 | 19 | 四方向落地方案 + 技能包格式 |
| D6 | 可观测/易用性/学习性 | 20 | "黑匣子打开"设计 + 小白旅程重设计 |

### C3/C4 关键结论（主项目当前真实状态，file:line 为准）

**架构（M1）**：OpenAI 主链路真闭环（api.rs:725-728 流式真增量）；**并发控制为零**（README 宣称的双桶信号量代码不存在）；usage token 恒 0（api.rs:678 传 0,0）；Claude 流式未转换（api.rs:858 透传 OpenAI SSE）；token 导入重启不回池；提示词/技能内存态无持久化；5 个配置项零消费；api.rs 单体 1147 行 + UI 内嵌字符串是扩展最大阻碍；多模态上传只差一个路由（web_protocol.rs:358 已实现）。
**UX（M2）**：README_zh.md 仍是 Go 旧版（P0，中文用户第一步即错）；面板 `{model_count}` 字面量 bug（api.rs:85 vs web.rs:73）；两个按钮把用户带离面板（web.rs:50-51）；DOM 无限堆积（web.rs:133-160）；无 token 导入 UI；请求 token/错误已入库但不展示；桌面端启动/登录失败零提示（main.js:158,189）。
**扩展接入点（M1 第 4 节）**：多模态输入（低风险，纯新增路由）；用户记忆（注入点现成 api.rs:618-623）；skills 沉淀（PromptManager 内存态→需持久化）；PPT/电商（需工具执行循环，高改动）。

### C6 深挖成果（D1–D6 已全部交付，落盘 ref_scan/D1-D6.md）

| ID | 核心交付 |
|----|---------|
| D1 | 12 个同构网关源码级提炼；主项目网关升级包 13 条（P0 4/P1 5/P2 4）；许可证红线标注（auth2api 无 LICENSE、new-api AGPL、tingly-box 商业许可 → 禁止拷代码） |
| D2 | 20 个技能项目提炼；完整技能体系设计（双源存储/生命周期状态机/roster+auto 注入/分发锁）；P0 4 项/P1 4 项/P2 5 项 |
| D3 | 18 个记忆项目提炼；记忆层设计（独立 memory.sqlite + trigram FTS5 + 零 LLM 规则 observe + 末尾注入保 KV-cache）；MVP ≈ 250-350 行 src/memory.rs |
| D4 | 18 个工作台项目提炼；三阶段演进（能力外露→任务工作台→多 agent/远程）；任务系统 SQLite schema 草案；工具执行三形态对比（推荐①skill 增强→②内置 loop） |
| D5 | 19 个垂直项目提炼；多模态输入 ≈200 行落地方案（代码 90% 已通）；PPT 四路线对比（推荐 OfficeCLI 执行层）；电商 staged 审批；统一技能包格式 |
| D6 | 21 个可观测项目提炼；"黑匣子"设计（telemetry.rs + logbus SSE + 请求详情抽屉 + doctor + 教学页）；真相修正：**usage token 恒 0、Claude 路径完全不记录** |

### C7 分层差距分析（现状 → 目标）

| 层 | 现状 | 差距 | 优先级 |
|----|------|------|--------|
| 协议层 | OpenAI 主链路真闭环；Claude 非流式可转换、**流式未转换**；无 Responses 协议 | D1 P0-3：canonical event + decoder/renderer 管线（Elysia 结构） | P0 |
| 稳定性 | 无请求级重试、无并发限流、冷却=简单时间戳、账号失败不分类 | D1 P0-2/4、P1-5/6/9：重试(committed 边界)+失败分类+熔断三态+池选择 | P0/P1 |
| 可观测 | token 恒 0；Claude 路径不记录；无事件表；面板 5 列 | D6：telemetry.rs + events 表 + logbus + 详情抽屉 + doctor | P0(采集)/P1(展示) |
| 技能 | 内存态 11 条、重启丢失、无自定义、全量注入 | D2：双源持久化 + 生命周期 + roster/auto 注入 + 评估 + 分发 | P0(持久化)/P1(蒸馏评估) |
| 记忆 | 无 | D3：memory.rs MVP（零 LLM observe + trigram 检索 + 末尾注入） | P1 |
| 易用性 | README_zh 旧版、面板 4 bug、无导入 UI、失败无提示 | M2/D6 P0 修复包（全部 S 成本） | P0 |
| 垂直能力 | 多模态差一个路由；PPT/电商/图片视频无 | D5：多模态接线(P0) → 技能包格式(P0) → PPT/生成/电商(P1) | P0/P1 |
| 工作台 | 无任务/工具循环/审批 | D4：MCP 暴露(P1) → 任务工作台 MVP(P2) | P2 |

### C7 Red Team 反证（高影响方案）

| 方案 | 被当事实的假设 | 最小实验 | 决策 |
|------|--------------|---------|------|
| Claude 流式转换 | "Claude 客户端必须收 Claude 事件流" | 用 Claude Code 实测当前透传行为（是否可用/哪些场景坏） | 先实测再定转换范围；若客户端容忍则仅补 usage/finish 事件 |
| 技能持久化+注入升级 | "用户会创建/使用自定义技能" | 先做文件化+导入导出+roster，观察 2 周使用率再投蒸馏/评估 | 缩小首批范围 |
| 记忆层注入 | "注入记忆不破坏上游行为/缓存" | 先只读展示（存但不注入），核对上游成功率/token 无变化后再开启注入 | 分两步上线 |
| 多模态上传 | "上游接受 OpenAI content 数组图片" | 用真实 Cookie 只读探测 1 次上传+1 次带图请求 | 探测后定 |
| PPT 生成 | "OfficeCLI 在本机可用且许可证兼容" | 本机验证二进制 + 跑一次 render→PNG | 验证后定 |
| MCP 暴露 | "用户需要 MCP" | 先暴露 3 个只读工具（models/usage/accounts），观察调用量 | 缩小范围 |
| 工作台 agent loop | "自建工具循环是正确形态" | 先验证"任务式技能+手动执行"是否已满足用户 | 推迟到 P2 |
| 上游生成端点 | "Freebuff 有图片/视频生成" | 在 reference/reverse 检索 + 只读探测端点 | 探测后定（不得付费调用） |

### C8 优先级路线图（P0/P1/P2）

**P0 — 先做（稳定性/核心体验/地基，全部 S-M 成本）**
| # | 做什么 | 参考 | 改动文件 | 验证 | 风险 |
|---|--------|------|---------|------|------|
| P0-1 | 小白可用性修复包：README_zh 重写、面板 4 bug（model_count/按钮/DOM 堆积/空态）、token 导入 UI、启动与登录失败弹窗 | M2/D6 | README_zh.md、src/web.rs、src/api.rs:85、desktop/main.js:158,189 | 面板实测 + curl | 无 |
| P0-2 | 用量统计真实化：流式 usage 采集（stream_options+tee）、Claude 路径记录、上游错误落库 | D6/D1 | src/usage.rs、src/api.rs、src/upstream.rs | 真实请求后查库 | 无（后续一切的地基） |
| P0-3 | Claude 流式协议转换（canonical event + SSE renderer），先做实测确定范围 | D1/Elysia | 新增 src/protocol/、src/api.rs:744 | 矩阵测试 + Claude Code 实测 | 中（事件面大，先 4 类） |
| P0-4 | 账号状态机+请求级重试（失败分类/指数退避/Retry-After/committed 边界） | D1/auth2api、free-claude-code | src/pool.rs、新增 src/retry.rs | 单测 + 模拟 401/429 | 低 |
| P0-5 | 技能持久化：src/skills/ 模块 + SQLite/文件双源 + roster 注入 + UI CRUD/导入导出 | D2 | 新增 src/skills/*，src/prompts.rs、src/api.rs、src/web.rs | 单测 + 重启不丢 | 低（分两步：先持久化不换注入） |
| P0-6 | 多模态上传接线：POST /v1/uploads + extract_multimodal + web chat 接线 | D5 | src/api.rs、Cargo.toml(multipart) | e2e 3 条（先探测上游） | 中（上游待验证） |
| P0-7 | 统一鉴权中间件（8+ handler 重复 → middleware） | D1 | src/api.rs | 定向测试 | 低 |

**P1 — 高价值改造**
| # | 做什么 | 参考 | 成本 |
|---|--------|------|------|
| P1-1 | 可观测套件：logbus + SSE 日志页 + 请求详情抽屉 + 人话解释层 + /api/doctor 体检 | D6 | M |
| P1-2 | 记忆层 MVP：memory.rs（独立 sqlite + trigram FTS5 + 零 LLM observe + 只读展示先上线） | D3 | M |
| P1-3 | 技能二期：AI 蒸馏向导 + 任务后结晶收件箱 + evals/棘轮 + 跨客户端同步（lock v2） | D2 | M |
| P1-4 | 网关增强：池选择升级（优先级层+加权随机）+ 错误规则表 + 熔断三态 + 消费 5 个零配置 + 并发限流 | D1 | M |
| P1-5 | MCP 最小暴露（models/accounts/usage/skills 只读工具，优先用 rmcp crate） | D4 | M |
| P1-6 | PPT 技能包：OfficeCLI 集成 + skills/ppt-maker + 统一技能包格式（contract.json/scripts/references） | D5 | M |
| P1-7 | 成本/配额可视化：burn rate（30min 滑窗）+ 预算告警 + /api/usage/cost（诚实标注 estimated） | D6 | M |

**P2 — 扩展（用户愿景的后续阶段）**
| # | 做什么 | 参考 |
|---|--------|------|
| P2-1 | 工作台 MVP：tasks/runs/events/approvals 四表 + 单 agent 工具循环（5 工具）+ 审批弹窗 + 任务板 | D4/codeg |
| P2-2 | 图片/视频生成：media.rs（submit→poll→download）+ 任务面板 + 技能包（探测上游后定形态） | D5 |
| P2-3 | 电商工具：商品文案/竞品分析（零资质）+ staged 审批 + 后续平台对接 | D5 |
| P2-4 | 多 agent/远程执行器 + IM 入口 + GenUI 导出 | D4 |
| P2-5 | 教学页：原理页（演进式章节）+ 术语表 + 交互式链路回放 | D6 |

### C8 Next Gate（待用户决策）

1. **实施授权**：是否批准 P0 批次（P0-1~P0-7）开始实施？建议先批准 P0-1/P0-2/P0-7（纯修复+地基，零风险），P0-3/P0-5/P0-6 需先做最小实验。
2. **扩展方向优先级**：图片视频 / 电商 / PPT 三个方向的排序（影响 P1-6 与 P2 排期）。
3. **上游能力探测授权**：是否允许用现有 Cookie 做只读探测（上传/生成端点是否存在）——不产生付费调用。
4. **架构决策**：桌面端是否评估 Tauri 迁移（Hermes-CN-Desktop/auto-subs 证明 Rust+Tauri 可复用现有后端），或维持 Electron。
5. **P0-3 范围确认**：Claude 流式转换先做多少类事件（建议 text/tool_use/usage/error 四类）。

---

# Phase D — 实施 / E2E 验证 / 独立审查 / 发布（2026-09-11）

## D. Task Contract

- **用户授权**（本轮原文摘要）："全部授权所有行为和阶段，完整落地闭环，真实 E2E 测验、验收、审计，提交推送到仓库创建发行版"。
- **实施范围**：Phase C 路线图 P0 全部（P0-1~P0-7）+ P1 可观测套件（logbus/SSE/详情/doctor）。P2（工作台 agent loop/多 agent/图片视频/电商）按 Red Team 结论保留为下一里程碑。
- **成功标准**：代码落地 + 单测/集成测试全绿 + clippy 零警告 + release 构建 + E2E 实测（含真实上游）+ 独立审查 + 提交推送发布。

## D. 实施批次（全部已落地）

| ID | 内容 | 交付物 | 状态 |
|----|------|--------|------|
| D1 | Claude 流式协议转换（canonical event + decoder + renderer） | src/protocol/{mod,stream,openai_sse,anthropic_sse}.rs（Builder-B1） | DONE |
| D2 | 技能持久化（文件真相源 + SQLite + roster 注入 + 质量门） | src/skills/{mod,frontmatter,store,inject,gate}.rs（Builder-B2） | DONE |
| D3 | 重试分类 / 日志总线 / 遥测写入 | src/retry.rs、src/logbus.rs、src/telemetry.rs（Builder-B3） | DONE |
| D4 | 主控接线：AppState/路由/handler（skills/logs/doctor/详情/uploads 端点；chat/claude/web 三链路 telemetry + 真实 token 采集；skills roster 注入；多模态 images 解析） | src/api.rs、src/main.rs、src/lib.rs、src/config.rs、src/usage.rs、src/prompts.rs | DONE |
| D5 | /ui 面板重写（修复 4 个旧 bug + 新增：token 导入 UI / 实时日志 SSE / 请求详情抽屉 / 技能库管理 / 系统体检 / 接入指南；tab 内切换；固定容器重建） | src/web.rs | DONE |
| D6 | 桌面壳修复（启动/登录失败弹窗、托盘配置/数据/日志/体检入口、网关日志落盘、自动更新闭环、端口检测、preload IPC） | desktop/main.js、desktop/preload.js、desktop/package.json | DONE |
| D7 | README_zh.md 重写（Rust 版 + 客户端接入指南 + FAQ） | README_zh.md | DONE |
| D8 | 真实缺陷修复：registry 网络同步无超时阻塞启动 | src/main.rs client_http 加 connect/total timeout | DONE |
| D9 | E2E 脚本（29 项断言，含面板元素/技能 CRUD/SSE/体检/鉴权/上传错误码） | tests/e2e_phase_d.cjs | DONE |

## D. Validation Matrix（实测证据）

| 验证项 | 命令/动作 | 结果 |
|--------|----------|------|
| 单元测试 | `cargo test` | ✅ 105 passed（原 24 → 新增 81：protocol 矩阵/skills 存储/retry/logbus/telemetry） |
| 集成测试 | `cargo test` | ✅ 8 passed |
| Lint 门禁 | `cargo clippy --all-targets -- -D warnings` | ✅ exit 0 零警告 |
| Release 构建 | `cargo build --release` | ✅ 10.8MB 单二进制 |
| E2E（隔离环境，无凭证） | `node tests/e2e_phase_d.cjs 47831` | ✅ 29/29（面板 10 项/技能 8 项/日志 3 项/体检 2 项/详情/上传/模型/鉴权） |
| 重启持久化 | 创建技能（含中文 id）→ 杀进程 → 重启 → 查询 | ✅ 技能与 enabled 状态保留；skills 目录文件落盘 |
| **真实上游 E2E** | POST /v1/web/chat（真实 Cookie，`glm-5.3-flash`） | ✅ HTTP 200 + SSE；77-78 chunks / 9.5KB；TTFT 9.7-12.1s |
| 真实遥测落地 | 上述请求后查 /api/logs/recent + /api/usage/requests | ✅ 日志含首字节/总耗时/字节数；usage 记录 account=web-cookie、latency=5439ms、status=200 |
| 体检端点真实数据 | GET /api/doctor | ✅ 四态正确（账号池 fault 因未配置账号；其余 ok/fact） |
| 启动健壮性（真实缺陷） | 断网场景启动 | ✅ 修复后 2s 内就绪（修复前阻塞 30s+） |

## D. Review Findings（3 位独立 Critic 完成，问题已修复并复验）

**Critic 报告**：ref_scan/C1.md（逻辑/需求）、C2.md（安全/并发）、C3.md（前端/文档）。共报告 1 BLOCKER + 9 MAJOR + 12 MINOR。

**已修复（复验证据）**：

| 发现 | 级别 | 修复 | 复验 |
|------|------|------|------|
| 流式 tail 按字节截断 → 多字节边界 panic（中文流必触发，遥测/用量静默丢失） | BLOCKER | 新增 `tail_keep()` 按字符边界安全截断（api.rs 两处） | 编译通过 + 全部流式测试通过 |
| /v1/web/chat 完全无鉴权 | MAJOR | 加 origin 校验 + api_keys 校验 + loopback 兜底 | E2E 9.1 同类校验通过 |
| CSRF：默认配置下网页可写管理端点 | MAJOR | `origin_allowed()`：带 Origin 且非同源一律拒绝 | 实测跨站 Origin → 401；同源 → 200 |
| 面板存储型 XSS（editSkill JSON-in-attribute） | MAJOR | 改为索引引用（skillsCache + editSkillAt/toggleSkillAt/delSkillAt），用户数据不进 JS 上下文 | 代码审查 + 面板 JS 语法检查 |
| 质量门只提示不拦截 + 前端未接线 | MAJOR | 服务端：gate(name/description/body) 非空默认 400（force 可强制）；前端：保存前调 /api/skills/gate 并展示 | 实测注入短语 → 400 + issues |
| /v1/uploads 声称 20MB 实际 2MB（413） | MAJOR | 路由加 `DefaultBodyLimit::max(20MB)` | 实测 3MB → 400（可达 handler，不再 413） |
| 请求详情事件链恒空（ts 当 req_id） | MAJOR | usage.requests 加 req_id 列（幂等迁移）+ 7 处 record 传 req_id + 生产路径 telemetry.event（route/upstream_ok/upstream_error）+ 详情按 req_id 关联 + 新增 telemetry 富字段读取 | 编译 + 真实链路遥测落库验证 |
| 流式 usage 从未请求（token 大概率恒 0） | MAJOR | stream:true 时注入 `stream_options.include_usage` | 待真实流式复测（见剩余项） |
| Anthropic 渲染器并行工具调用协议非法 | MAJOR | 缓冲式渲染（flush_tools，B1 重写 + 3 新测试） | protocol 测试集全过 |
| 面板导入 curl/HAR 失效 + 绕过跨域校验（含 Chrome 单引号格式 host 解析失败） | MAJOR | 后端兼容 JSON 包裹（{cookie}/{text}）；parse_curl 支持单引号位置参数 URL + host/path 正确解析 | 新增 2 测试（chrome 格式含跨域拒绝）+ 实测导入 host 正确 |
| 面板 api_keys 配置后 401（功能死亡） | MAJOR | 面板顶部 API Key 输入（localStorage）+ api() 注入 Authorization + SSE `?key=` | 代码审查 |
| 桌面 userData 路径与文档不符 | MAJOR | package.json 加顶层 productName + 旧目录自动迁移 | 代码审查 |
| tokens.json 硬编码不可配置（E2E 无法隔离） | MAJOR | 新增 `tokens_path` 配置（默认 data/tokens.json） | 隔离环境 E2E 29/29 通过 |
| mask() 多字节 panic | MINOR | 改为按字符切分 | 既有测试 + 代码审查 |
| import.rs parse_path 索引错误 | MINOR | 重写为 find("://")+3 后取 '/' 起 | 新测试 path 断言 |
| SSE 断线丢事件 | MINOR | 读 Last-Event-ID → recent(limit, Some(id)) 补发 | 代码审查 |
| loadBalance 引导分支不可达 | MINOR | 改为先判 r.ok 再展示引导 | 代码审查 |
| roster 预算硬编码 2000 | MINOR | API 返回 max_roster_tokens，前端读取 | 代码审查 |
| 内置技能编辑文案矛盾 | MINOR | 前端内置项隐藏编辑/删除，显示"内置项不可编辑" | 代码审查 |
| 托盘 #doctor 无路由 | MINOR | web.rs 支持 location.hash 定位 tab | 代码审查 |
| README 技能导入导出/体检描述/端点表不实 | MINOR | README_zh 全面校正（技能节/端点表/FAQ） | 文档审查 |
| 版本漂移（lock/README/API_GUIDE 0.2.0） | MINOR | 全部统一 0.3.0 | grep 验证 |

**未修复 / 已知限制（如实披露）**：
- 流式 include_usage 已注入但**未用真实上游复测**（真实流式请求验证列为发布后跟测项）。
- 成功路径的细粒度事件链目前 3 个节点（route/upstream_ok/error），更细的"账号选择理由/重试"事件为 P2 增强。
- retry.rs 的退避函数仍未接入请求级重试循环（仅 classify_status 用于错误分类）。
- skills 并发写（文件/DB/缓存三方）未加事务锁——管理端点并发概率低，列为 P2。
- telemetry 强杀时可能丢队列内数据（4296 条上限）；未接优雅停机信号。
- /v1/web/chat 上游不返回 usage → token 记 0（延迟/字节/首字节正常）。
- api_keys 空 + 监听 0.0.0.0 的场景仍需用户自行加 key（文档已提示；origin 校验已挡住浏览器侧 CSRF）。

## D. 验证复跑记录（修复后）

| 验证 | 结果 |
|------|------|
| `cargo test` | ✅ **108 单测 + 8 集成全过**（新增 chrome curl 2 项、protocol 并行工具 3 项） |
| `cargo clippy --all-targets -- -D warnings` | ✅ 零警告 |
| E2E 29 项（隔离环境） | ✅ 29/29 |
| Critic 修复专项验证 | ✅ 质量门 400+issues / 3MB 上传不 413 / JSON 包裹导入 host 正确 / 跨站 Origin 401 / 同源 200 |

## C. Scope Notes（本轮边界）

- 主项目当前是"API 网关 + 桌面壳 + 提示词/技能注入"，**不是完整 agent 应用**；用户愿景是从网关向"AI 工作台/agent 平台"演进。路线图需明确：哪些愿景可基于现有架构渐进落地、哪些是独立新产品线。
- 扩展方向（图片视频/电商/PPT）的落地形态需与用户确认（网关代理 vs 内置工作台 UI vs 独立 skill 包），本阶段先给候选方案对比。

## C. Evidence Ledger（Phase C 追加）

| Claim | Evidence | Type | Confidence |
|-------|----------|------|------------|
| 参考项目共 957 个一级目录（+1 个 .md 文件） | ls 计数；分组文件 32 组 | DIRECT | 高 |
| 主项目已具备提示词/技能注入（system 前缀） | src/prompts.rs、src/web.rs:132-139 | DIRECT | 高 |
| 主项目 /ui 为内嵌 HTML/JS 字符串单文件 | src/web.rs 176 行 | DIRECT | 高 |
| 上一轮 958 目录扫描为摘要级，未落盘明细 | 状态文件原文"各 Scout 摘要"；磁盘无扫描结果文件 | DIRECT | 高 |
| 桌面壳为 Electron（托盘/一键登录/自动更新） | desktop/main.js 247 行、desktop/package.json | DIRECT | 高 |
| （待补）Phase C 扫描聚合结果 | C2/C3/C4 交付后追加 | UNVERIFIED | — |
